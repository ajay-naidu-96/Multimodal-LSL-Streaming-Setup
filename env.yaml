name: lsl
channels:
  - defaults
  - conda-forge
dependencies:
  # Core LSL requirements
  - python=3.11
  - pip
  - pip:
      - pylsl>=1.16.0
      - PyYAML>=6.0
      - opencv-python>=4.8.0
      - pyaudio>=0.2.11
      - pyserial>=3.5
      - numpy>=1.24.0
      - python-can>=4.2.0

  # Optional: Intel RealSense support
  # - pip:
  #     - pyrealsense2>=2.54.0

  # Optional: Additional sensor libraries
  # - pip:
  #     - bluetooth-serial

  # Development/Testing
  - matplotlib>=3.7.0
  - pandas>=2.0.0
